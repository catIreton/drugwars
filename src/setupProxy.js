// This file configures webpack-dev-server middleware
// CRA will automatically use this if present

module.exports = function(app) {
  // Custom middleware can be added here
  // By creating this file, we prevent CRA from using deprecated middleware options
};
